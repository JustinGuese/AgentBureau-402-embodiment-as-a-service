{
  "name": "AgentBureau",
  "version": "1.0.0",
  "description": "The Notary for the Agentic Age. Physical embodiment for AI agents.",
  "tools": [
    {
      "name": "send_letter",
      "description": "Send a physical letter via Deutsche Post. Required for legal notices (BGB Section 126).",
      "parameters": {
        "type": "object",
        "properties": {
          "recipient": { "type": "string" },
          "address": { "type": "string" },
          "content": { "type": "string" },
          "type": { "enum": ["standard", "registered"], "default": "standard" }
        },
        "required": ["recipient", "address", "content"]
      }
    },
    {
      "name": "send_fax",
      "description": "Send a digital fax to a German number. Provides a legally valid transmission report.",
      "parameters": {
        "type": "object",
        "properties": {
          "recipient_number": { "type": "string" },
          "content": { "type": "string" }
        },
        "required": ["recipient_number", "content"]
      }
    },
    {
      "name": "create_invoice",
      "description": "Generate a GoBD-compliant German invoice with automated VAT calculation.",
      "parameters": {
        "type": "object",
        "properties": {
          "recipient": { "type": "object" },
          "items": { "type": "array" },
          "tax_rate": { "type": "number" }
        },
        "required": ["recipient", "items"]
      }
    },
    {
      "name": "form_company",
      "description": "Initiate the formation of a German UG or GmbH for an AI agent.",
      "parameters": {
        "type": "object",
        "properties": {
          "company_name": { "type": "string" },
          "type": { "enum": ["UG", "GmbH"] }
        },
        "required": ["company_name", "type"]
      }
    }
  ],
  "payment": {
    "protocol": "x402",
    "network": "base",
    "currency": "USDC"
  }
}